// api/utils/normalMap.js - МИНИМАЛЬНАЯ ВЕРСИЯ
export function generateNormalMap(textureCanvas) {
    console.log('⚠️ Normal Map отключена (заглушка)');
    return textureCanvas; // Просто возвращаем исходный canvas
}
